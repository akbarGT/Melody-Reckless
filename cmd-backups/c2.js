 
    // const msgArray = msg.content.split(' ');
	// const args2 = msgArray.slice(1)(' ');

    // if (!msg.member.permissions.has("MANAGE_MESSAGES")) return msg.channel.send('Lack of Perms!');
    
    // let deleteAmount;

    // if (isNaN(args2[0]) || parseInt(args2[0]) <= 0) { return msg.reply('Please put a number only!') }

    // if (parseInt(args2[0]) > 100) {
    //     return msg.reply('You can only delete 100 messages at a time!')
    // } else {
    //     deleteAmount = parseInt(args2[0]);
    // }

    // msg.channel.bulkDelete(deleteAmount + 1, true);
    // msg.reply(`**Successfully** Deleted ***${deleteAmount}*** Messages.`)